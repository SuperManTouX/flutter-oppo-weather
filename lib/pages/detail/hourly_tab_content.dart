import 'package:flutter/material.dart';

class HourlyTabContent extends StatelessWidget {
  const HourlyTabContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('首页内容'));
  }
}